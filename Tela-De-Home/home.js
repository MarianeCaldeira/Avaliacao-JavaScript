/*Se algum link nao foi criado, deverá colocar a posição dentro do array*/
var pagina = document.getElementsByTagName("a")[0];
pagina.addEventListener("click", () => {
    pagina = pagina.getAttribute("href");
    if (pagina === "#"){
        alert("A tela não foi encontrada. Entre em contato com o restaurante.");
    }
})

var pagina = document.getElementsByTagName("a")[1];
pagina.addEventListener("click", () => {
    pagina = pagina.getAttribute("href");
    if (pagina === "#"){
        alert("A tela não foi encontrada. Entre em contato com o restaurante.");
    }
})

var pagina = document.getElementsByTagName("a")[2];
pagina.addEventListener("click", () => {
    pagina = pagina.getAttribute("href");
    if (pagina === "#"){
        alert("A tela não foi encontrada. Entre em contato com o restaurante.");
    }
})

var pagina = document.getElementsByTagName("a")[3];
pagina.addEventListener("click", () => {
    pagina = pagina.getAttribute("href");
    if (pagina === "#"){
        alert("A tela não foi encontrada. Entre em contato com o restaurante.");
    }
})

var pagina = document.getElementsByTagName("a")[4];
pagina.addEventListener("click", () => {
    pagina = pagina.getAttribute("href");
    if (pagina === "#"){
        alert("A tela não foi encontrada. Entre em contato com o restaurante.");
    }
})